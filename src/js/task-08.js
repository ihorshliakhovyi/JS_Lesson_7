'use strict';

const controls = document.getElementById('controls');
const divReciever = document.getElementById('boxes');



function createBoxes(amount) {
    if (amount > 0) {  
        do {
        const box = document.createElement('div');
    } while (amount !== amount); 
    };
};


controls.addEventListener('click', createBoxes);

console.log(divReciever);